# docker-compose-rails-pg
repo to house files/content associated with building a rails, pg app with docker

## Generating the skeleton app

`docker-compose run web rails new . --force --database=postgresql`
